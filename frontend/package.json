{
  "name": "luxvira-scents-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0"
  }
}
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    // Add this for Render static serving:
    "start": "serve -s dist -l $PORT"
  },
  "dependencies": {
    // ... existing deps ...
    "serve": "^14.2.1"  // ← Add this for static file serving
  }
}
